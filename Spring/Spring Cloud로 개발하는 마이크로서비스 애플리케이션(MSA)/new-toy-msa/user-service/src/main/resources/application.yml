server:
  port: 0

spring:
  application:
    name: user-service
#  config:
#    import: "optional:configserver:http://localhost:8888"
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: user
      password: $2a$10$sV.2eUObxZdqN0LIRJMTGeq/M7d0Gf/NdEintpquo8i0QOy8sNuvW


eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}} # instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

greeting:
  message: Welcome to the Simple E-commerce.

#token:
#  expiration_time: 86400000
#  secret: my-very-long-secret-key-123456780123456

management:
  endpoints:
    web:
      exposure:
        include:
          - refresh
          - health
          - beans
          - info